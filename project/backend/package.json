{
    "name": "bookmark-management",
    "version": "1.0.0",
    "description": "一个简单的书签管理系统后端服务",
    "main": "server.js",
    "scripts": {
      "start": "node server.js", // 启动生产环境的服务器
      "dev": "nodemon server.js", // 开发模式下启用热加载
      "test": "echo \"没有测试用例\" && exit 0", // 占位测试命令，可替换为实际测试命令
      "lint": "eslint .", // 检查代码格式
      "prepare": "npm run build", // 准备发布（扩展脚本）
      "build": "echo \"构建逻辑在此处实现\" && exit 0" // 占位的构建脚本
    },
    "dependencies": {
      "express": "^4.18.2", // Web框架
      "body-parser": "^1.20.1", // 解析请求体
      "sqlite3": "^5.1.6", // 数据库模块
      "dotenv": "^16.0.3" // 环境变量管理
    },
    "devDependencies": {
      "nodemon": "^2.0.22", // 开发模式下的热加载
      "eslint": "^8.38.0" // 代码格式检查工具
    },
    "engines": {
      "node": ">=16.0.0" // 指定Node.js的版本范围，避免运行时出错
    },
    "keywords": [
      "书签管理",
      "Node.js",
      "Express",
      "SQLite"
    ],
    "repository": {
      "type": "git",
      "url": "https://github.com/your-username/bookmark-management.git" // 替换为您的Git仓库地址
    },
    "author": "ZT",
    "license": "MIT"
  }
  
  